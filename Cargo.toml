[package]
name = "calculator"
version = "0.1.0-RC"
authors = ["Florian Gamböck <mail@floga.de>"]
edition = "2021"

[dependencies]
add.workspace = true
sub.workspace = true
mul.workspace = true
div.workspace = true

[workspace]
members = [
    "add",
    "sub",
    "mul",
    "div",
]

[workspace.dependencies]
add = { version = "0.1.0-RC", path = "add" }
sub = { version = "0.1.0-RC", path = "sub" }
mul = { version = "0.1.0-RC", path = "mul" }
div = { version = "0.1.0-RC", path = "div" }
